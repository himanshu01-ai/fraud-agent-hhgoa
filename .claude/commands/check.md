---
description: Run the full check loop (benchmark, validator, tests, case view)
---
Run the check loop from CLAUDE.md with the backend the user names (default local). Print the 20-case verdict table,
validator result, pytest result, and list any case whose verdict/pattern/actions changed since the last run
(compare with git diff of cases/). Do not change code unless asked.
